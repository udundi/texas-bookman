<div
  class="
    {% if block.settings.visibility contains 'mobile' %}
      tw-flex
      md:tw-hidden
    {% elsif block.settings.visibility contains "desktop" %}
      tw-hidden
      md:tw-flex
    {% else %}
      tw-flex
    {% endif %}
    tw-text-offblack/60 tw-gap-2
  "
>
  {% if product.metafields.facts.isbn != blank %}
    <div><span class="tw-font-semibold">ISBN:</span> {{ product.metafields.facts.isbn }}</div>
  {% endif %}
  {% if product.metafields.custom.tb_code != blank %}
    <div><span class="tw-font-semibold">TB Code:</span> {{ product.metafields.custom.tb_code }}</div>
  {% endif %}
</div>

{% schema %}
{
  "name": "PDP Info Numbers",
  "class": "tw-contents",
  "settings": [
    {
      "type": "select",
      "id": "visibility",
      "label": "Visibility",
      "options": [
        {
          "value": "both",
          "label": "Mobile and Desktop"
        },
        {
          "value": "mobile",
          "label": "Mobile only"
        },
        {
          "value": "desktop",
          "label": "Desktop only"
        }
      ],
      "default": "both"
    }
  ],
  "presets": [
    {
      "name": "PDP Info Numbers"
    }
  ]
}
{% endschema %}
